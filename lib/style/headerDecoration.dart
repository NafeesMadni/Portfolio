import 'package:flutter/material.dart';

final KHeaderDecoration = BoxDecoration(
  borderRadius: BorderRadius.circular(100),
  gradient: const LinearGradient(
    colors: [
      Colors.transparent,
      Color(0xff23153b),
    ],
  ),
);
